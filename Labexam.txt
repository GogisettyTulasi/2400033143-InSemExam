cd <path>
git init
code .
git branch
git status
git checkout -b feature1
 function creation
git add .
git commit -m"Commit after changes by alice"
git checkout -b feature2
 function modification
git add .
git commit -m"Commit after changes by bob"
git checkout master
git merge feature1
git merge feature2
git add .
git commit -m"final commit after merging"
git add .
git commit -m"final commit after resolving the error"
